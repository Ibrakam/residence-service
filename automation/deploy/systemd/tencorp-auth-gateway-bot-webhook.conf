# Install only as an explicit systemd drop-in when the optional Telegram
# message webhook is enabled. The base gateway unit never inherits the Bot API
# token, even if a bot.env file exists for the profile configuration script.
[Service]
EnvironmentFile=/etc/tencorp-auth-gateway/bot.env
Environment=AUTH_BOT_WEBHOOK_ENABLED=true
