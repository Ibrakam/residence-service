'use client';

/* eslint-disable @next/next/no-img-element */

import { type KeyboardEvent as ReactKeyboardEvent, useEffect, useMemo, useRef, useState } from 'react';
import { LeadModal } from '@/app/lead-modal';
import type snapshotData from '@/data/4u-catalog.json';

type Language = 'ru' | 'uz' | 'en';
type Mode = 'cards' | 'chess';
type Sort = 'price-asc' | 'price-desc' | 'area-asc' | 'area-desc' | 'floor-asc';
type Unit = (typeof snapshotData.units)[number];
type Snapshot = typeof snapshotData;

const appBasePath = process.env.NEXT_PUBLIC_APP_BASE_PATH ?? '';
const languages: Language[] = ['ru', 'uz', 'en'];

const copy = {
  ru: {
    back: 'О проекте', kicker: '4U TASHKENT · OFFICIAL SNAPSHOT', title: 'Выберите свою', accent: 'квартиру.', lead: '33 реальных предложения из 183 объектов официального каталога на дату snapshot. Цены и статусы могут измениться — менеджер подтвердит актуальность.', snapshot: 'Snapshot · 30.08.2026', update: 'Источник проверен 30 августа 2026', filters: 'Фильтры', phase: 'Очередь', allPhases: 'Все очереди', type: 'Тип', apartment: 'Квартира', rooms: 'Комнаты', any: 'Все', area: 'Площадь, м²', price: 'Цена, млн UZS', completion: 'Срок очереди', allDates: 'Все сроки', status: 'Статус', available: 'В продаже', sort: 'Сортировка', sorting: { 'price-asc': 'Сначала дешевле', 'price-desc': 'Сначала дороже', 'area-asc': 'Площадь ↑', 'area-desc': 'Площадь ↓', 'floor-asc': 'Этаж ↑' }, reset: 'Сбросить', cards: 'Карточки', chess: 'Шахматка', found: 'найдено', unit: 'Квартира', floor: 'Этаж', entrance: 'Подъезд', perM2: 'за м²', oldPrice: 'Цена до акции', statusNote: 'Статус на дату snapshot', details: 'Подробнее', consult: 'Уточнить условия', showMore: 'Показать ещё', shown: 'Показано', of: 'из', empty: 'По выбранным фильтрам квартир нет.', snapshotNote: 'Локальный snapshot содержит стратифицированную выборку по всем доступным очередям и комнатности. Официальный каталог показывал 183 предложения.', tableHelp: 'Прокручивайте таблицу пальцем или кнопками.', left: 'Прокрутить влево', right: 'Прокрутить вправо', noSelection: 'Выберите квартиру в шахматке', close: 'Закрыть', zoom: 'Открыть планировку', plan: 'Реальная планировка из официального каталога', language: 'Язык', context: 'Каталог 4U Tashkent', phaseShort: 'Очередь', completionShort: 'Сдача', menu: 'Меню каталога',
  },
  uz: {
    back: 'Loyiha haqida', kicker: '4U TASHKENT · OFFICIAL SNAPSHOT', title: 'Xonadoningizni', accent: 'tanlang.', lead: 'Snapshot sanasidagi 183 rasmiy obyektning 33 ta haqiqiy taklifi. Narx va holat o‘zgarishi mumkin.', snapshot: 'Snapshot · 30.08.2026', update: 'Manba 2026-yil 30-avgustda tekshirildi', filters: 'Filtrlar', phase: 'Navbat', allPhases: 'Barcha navbatlar', type: 'Tur', apartment: 'Xonadon', rooms: 'Xonalar', any: 'Barchasi', area: 'Maydon, m²', price: 'Narx, mln UZS', completion: 'Topshirish muddati', allDates: 'Barcha muddatlar', status: 'Holat', available: 'Sotuvda', sort: 'Saralash', sorting: { 'price-asc': 'Arzonidan', 'price-desc': 'Qimmatidan', 'area-asc': 'Maydon ↑', 'area-desc': 'Maydon ↓', 'floor-asc': 'Qavat ↑' }, reset: 'Tozalash', cards: 'Kartalar', chess: 'Shaxmatka', found: 'topildi', unit: 'Xonadon', floor: 'Qavat', entrance: 'Kirish', perM2: '1 m² uchun', oldPrice: 'Aksiyagacha', statusNote: 'Snapshot sanasidagi holat', details: 'Batafsil', consult: 'Shartlarni aniqlash', showMore: 'Yana ko‘rsatish', shown: 'Ko‘rsatildi', of: 'dan', empty: 'Tanlangan filtrlarga mos xonadon yo‘q.', snapshotNote: 'Mahalliy snapshot barcha navbat va xona sonlari bo‘yicha tanlovni saqlaydi. Rasmiy katalogda 183 taklif bor edi.', tableHelp: 'Jadvalni barmoq yoki tugmalar bilan aylantiring.', left: 'Chapga', right: 'O‘ngga', noSelection: 'Shaxmatkadan xonadon tanlang', close: 'Yopish', zoom: 'Rejani ochish', plan: 'Rasmiy katalogdagi haqiqiy reja', language: 'Til', context: '4U Tashkent katalogi', phaseShort: 'Navbat', completionShort: 'Topshirish', menu: 'Katalog menyusi',
  },
  en: {
    back: 'About the project', kicker: '4U TASHKENT · OFFICIAL SNAPSHOT', title: 'Choose your', accent: 'apartment.', lead: '33 verified offers sampled from 183 official listings at snapshot date. Prices and availability can change.', snapshot: 'Snapshot · 30 Aug 2026', update: 'Source verified on 30 August 2026', filters: 'Filters', phase: 'Phase', allPhases: 'All phases', type: 'Type', apartment: 'Apartment', rooms: 'Rooms', any: 'Any', area: 'Area, m²', price: 'Price, million UZS', completion: 'Phase completion', allDates: 'All dates', status: 'Status', available: 'Available', sort: 'Sort', sorting: { 'price-asc': 'Lowest price', 'price-desc': 'Highest price', 'area-asc': 'Area ↑', 'area-desc': 'Area ↓', 'floor-asc': 'Floor ↑' }, reset: 'Reset', cards: 'Cards', chess: 'Chessboard', found: 'found', unit: 'Apartment', floor: 'Floor', entrance: 'Entrance', perM2: 'per m²', oldPrice: 'Before offer', statusNote: 'Status at snapshot date', details: 'Details', consult: 'Check terms', showMore: 'Show more', shown: 'Shown', of: 'of', empty: 'No apartments match these filters.', snapshotNote: 'The local snapshot is stratified across every available phase and room count. The official catalogue showed 183 listings.', tableHelp: 'Swipe the table or use the scroll buttons.', left: 'Scroll left', right: 'Scroll right', noSelection: 'Select an apartment on the chessboard', close: 'Close', zoom: 'Open plan', plan: 'Verified plan from the official catalogue', language: 'Language', context: '4U Tashkent catalogue', phaseShort: 'Phase', completionShort: 'Completion', menu: 'Catalogue menu',
  },
} as const;

const plan = (unit: Unit) => `${appBasePath}/4u/plans/${unit.id}.webp`;
const formatPrice = (value: number, language: Language) => `${new Intl.NumberFormat(language === 'uz' ? 'uz-UZ' : language === 'en' ? 'en-US' : 'ru-RU').format(value)} UZS`;
const phaseLabel = (value: string) => value.replace('4U Tashkent ', '').replace('1 - ', '1-');
function withLanguage(path: string, language: Language) { return `${appBasePath}${path}?lang=${language}`; }
function leadSubmitUrl() { return `${appBasePath}/v1/leads`; }

function useLanguage(initialLanguage: Language = 'ru') {
  const [language, setLanguageState] = useState<Language>(initialLanguage);
  useEffect(() => { const query = new URLSearchParams(location.search).get('lang'); const saved = localStorage.getItem('fouru-language'); const next = languages.includes(query as Language) ? query : saved; const frame = requestAnimationFrame(() => { if (languages.includes(next as Language)) setLanguageState(next as Language); }); return () => cancelAnimationFrame(frame); }, []);
  useEffect(() => { document.documentElement.lang = language; }, [language]);
  const setLanguage = (next: Language) => { setLanguageState(next); localStorage.setItem('fouru-language', next); const url = new URL(location.href); url.searchParams.set('lang', next); history.replaceState({}, '', url); };
  return [language, setLanguage] as const;
}

function UnitDetail({ unit, language, compact = false, onPlan, onConsult }: { unit: Unit; language: Language; compact?: boolean; onPlan: () => void; onConsult: () => void }) {
  const t = copy[language];
  return <article className={`fouru-unit-detail ${compact ? 'is-compact' : ''}`}><button className="fouru-unit-detail__plan" type="button" onClick={onPlan} aria-label={t.zoom}><img src={plan(unit)} alt={`${t.plan} №${unit.number}`} loading="lazy" /><span>↗</span></button><div className="fouru-unit-detail__head"><span>{t.available}</span><small>{phaseLabel(unit.phase)}</small><h3>{t.unit} №{unit.number}</h3><strong>{unit.rooms} · {unit.area} m²</strong></div><dl><div><dt>{t.floor}</dt><dd>{unit.floor} / {unit.maxFloor}</dd></div><div><dt>{t.entrance}</dt><dd>{unit.entrance}</dd></div><div><dt>{t.completionShort}</dt><dd>{unit.completion}</dd></div><div><dt>{t.perM2}</dt><dd>{formatPrice(Math.round(unit.price / unit.area), language)}</dd></div></dl><div className="fouru-unit-detail__price"><small>{t.statusNote}</small><strong>{formatPrice(unit.price, language)}</strong><del>{formatPrice(unit.oldPrice, language)}</del></div><button className="fouru-catalog-button is-primary" type="button" onClick={onConsult}>{t.consult}<span>↗</span></button></article>;
}

function PlanLightbox({ unit, language, onClose, onConsult }: { unit: Unit; language: Language; onClose: () => void; onConsult: () => void }) {
  const t = copy[language]; const closeRef = useRef<HTMLButtonElement>(null);
  useEffect(() => { closeRef.current?.focus(); document.body.classList.add('is-fouru-overlay'); const key = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); }; addEventListener('keydown', key); return () => { document.body.classList.remove('is-fouru-overlay'); removeEventListener('keydown', key); }; }, [onClose]);
  return <div className="fouru-planbox" role="dialog" aria-modal="true" aria-label={`${t.unit} №${unit.number}`} onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}><button ref={closeRef} className="fouru-planbox__close" type="button" aria-label={t.close} onClick={onClose}>×</button><div className="fouru-planbox__visual"><img src={plan(unit)} alt={`${t.plan} №${unit.number}`} /><span>{t.plan}</span></div><aside><span>{phaseLabel(unit.phase)}</span><h2>{t.unit} №{unit.number}</h2><p>{unit.rooms} · {unit.area} m² · {unit.floor}/{unit.maxFloor} {t.floor.toLowerCase()}</p><strong>{formatPrice(unit.price, language)}</strong><small>{formatPrice(Math.round(unit.price / unit.area), language)} {t.perM2}</small><button className="fouru-catalog-button is-primary" type="button" onClick={onConsult}>{t.consult}<span>↗</span></button></aside></div>;
}

export function FourUCatalog({ snapshot: snapshotData, initialLanguage = 'ru' }: { snapshot: Snapshot; initialLanguage?: Language }) {
  const [language, setLanguage] = useLanguage(initialLanguage); const t = copy[language];
  const [mode, setMode] = useState<Mode>('cards'); const [phase, setPhase] = useState('all'); const [rooms, setRooms] = useState<number | 'all'>('all'); const [minArea, setMinArea] = useState(33); const [maxArea, setMaxArea] = useState(108); const [minPrice, setMinPrice] = useState(800); const [maxPrice, setMaxPrice] = useState(2500); const [completion, setCompletion] = useState('all'); const [sort, setSort] = useState<Sort>('price-asc'); const [visible, setVisible] = useState(12); const [selected, setSelected] = useState<Unit | null>(snapshotData.units[0]); const [planbox, setPlanbox] = useState<Unit | null>(null); const [lead, setLead] = useState(false); const tableRef = useRef<HTMLDivElement>(null);
  const phases = useMemo(() => [...new Set(snapshotData.units.map((unit) => unit.phase))], [snapshotData.units]); const dates = useMemo(() => [...new Set(snapshotData.units.map((unit) => unit.completion))], [snapshotData.units]);
  const filtered = useMemo(() => snapshotData.units.filter((unit) => (phase === 'all' || unit.phase === phase) && (rooms === 'all' || unit.rooms === rooms) && unit.area >= minArea && unit.area <= maxArea && unit.price / 1_000_000 >= minPrice && unit.price / 1_000_000 <= maxPrice && (completion === 'all' || unit.completion === completion)).sort((a, b) => sort === 'price-asc' ? a.price - b.price : sort === 'price-desc' ? b.price - a.price : sort === 'area-asc' ? a.area - b.area : sort === 'area-desc' ? b.area - a.area : a.floor - b.floor), [snapshotData.units, phase, rooms, minArea, maxArea, minPrice, maxPrice, completion, sort]);
  useEffect(() => { const frame = requestAnimationFrame(() => setVisible(12)); return () => cancelAnimationFrame(frame); }, [phase, rooms, minArea, maxArea, minPrice, maxPrice, completion, sort]);
  const effectiveSelected = selected && filtered.some((unit) => unit.id === selected.id) ? selected : filtered[0] || null;
  const floors = useMemo(() => [...new Set(filtered.map((unit) => unit.floor))].sort((a, b) => b - a), [filtered]); const columns = useMemo(() => [...new Set(filtered.map((unit) => `${unit.phase}|${unit.entrance}`))], [filtered]);
  const resetFilters = () => { setPhase('all'); setRooms('all'); setMinArea(33); setMaxArea(108); setMinPrice(800); setMaxPrice(2500); setCompletion('all'); setSort('price-asc'); setVisible(12); };
  const consult = (unit?: Unit | null) => { if (unit) setSelected(unit); setPlanbox(null); setLead(true); };
  const scrollChess = (left: number) => tableRef.current?.scrollBy({ left, behavior: window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth' });
  const onChessKeyDown = (event: ReactKeyboardEvent<HTMLDivElement>) => {
    const table = tableRef.current;
    if (!table) return;
    if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') { event.preventDefault(); scrollChess(event.key === 'ArrowLeft' ? -420 : 420); }
    else if (event.key === 'Home' || event.key === 'End') { event.preventDefault(); table.scrollTo({ left: event.key === 'Home' ? 0 : table.scrollWidth, behavior: window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth' }); }
  };

  return <main className="fouru-catalog-site" lang={language}>
    <header className="fouru-catalog-header"><a className="fouru-logo" href={withLanguage('/4u', language)}><b>4U</b><span>TASHKENT</span></a><a href={withLanguage('/4u', language)} className="fouru-catalog-header__back">← {t.back}</a><div className="fouru-catalog-header__languages" aria-label={t.language}>{languages.map((item) => <a key={item} href={withLanguage('/4u/apartments', item)} className={language === item ? 'is-active' : ''} onClick={(e) => { e.preventDefault(); setLanguage(item); }}>{item.toUpperCase()}</a>)}</div><a className="fouru-catalog-header__cta" href="tel:1360">1360</a></header>
    <section className="fouru-catalog-hero"><span className="fouru-kicker">{t.kicker}</span><h1>{t.title} <em>{t.accent}</em></h1><p>{t.lead}</p><div><span>{t.snapshot}</span><span>{snapshotData.units.length} / {snapshotData.officialTotalAtCapture}</span></div></section>
    <section className="fouru-filter-panel" aria-label={t.filters}><header><h2>{t.filters}</h2><span>{filtered.length} {t.found}</span><button type="button" onClick={() => resetFilters()}>{t.reset}</button></header><div className="fouru-filter-grid"><label><span>{t.phase}</span><select value={phase} onChange={(e) => setPhase(e.target.value)}><option value="all">{t.allPhases}</option>{phases.map((item) => <option key={item} value={item}>{phaseLabel(item)}</option>)}</select></label><label><span>{t.type}</span><select disabled><option>{t.apartment}</option></select></label><fieldset><legend>{t.rooms}</legend><div>{(['all', 1, 2, 3, 4] as const).map((value) => <button key={value} type="button" className={rooms === value ? 'is-active' : ''} onClick={() => setRooms(value)}>{value === 'all' ? t.any : value}</button>)}</div></fieldset><label><span>{t.completion}</span><select value={completion} onChange={(e) => setCompletion(e.target.value)}><option value="all">{t.allDates}</option>{dates.map((item) => <option key={item}>{item}</option>)}</select></label><label className="fouru-range"><span>{t.area}</span><div><input type="number" inputMode="decimal" min="30" max={maxArea} value={minArea} onChange={(e) => setMinArea(Number(e.target.value))} aria-label={`${t.area} min`} /><i>—</i><input type="number" inputMode="decimal" min={minArea} max="131" value={maxArea} onChange={(e) => setMaxArea(Number(e.target.value))} aria-label={`${t.area} max`} /></div></label><label className="fouru-range"><span>{t.price}</span><div><input type="number" inputMode="numeric" min="0" max={maxPrice} value={minPrice} onChange={(e) => setMinPrice(Number(e.target.value))} aria-label={`${t.price} min`} /><i>—</i><input type="number" inputMode="numeric" min={minPrice} value={maxPrice} onChange={(e) => setMaxPrice(Number(e.target.value))} aria-label={`${t.price} max`} /></div></label></div></section>
    <div className="fouru-catalog-toolbar"><div className="fouru-modes" role="radiogroup" aria-label={t.menu}>{([['cards', t.cards], ['chess', t.chess]] as const).map(([value, label]) => <button type="button" role="radio" aria-checked={mode === value} className={mode === value ? 'is-active' : ''} onClick={() => setMode(value)} key={value}>{label}</button>)}</div><label><span>{t.sort}</span><select value={sort} onChange={(e) => setSort(e.target.value as Sort)}>{Object.entries(t.sorting).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label></div>

    {mode === 'cards' ? <section className="fouru-unit-grid" aria-live="polite">{filtered.slice(0, visible).map((unit) => <article className="fouru-unit-card" key={unit.id}><button className="fouru-unit-card__plan" type="button" onClick={() => setPlanbox(unit)} aria-label={`${t.zoom} №${unit.number}`}><img src={plan(unit)} alt={`${t.plan} №${unit.number}`} loading="lazy" decoding="async" /><span>↗</span></button><div className="fouru-unit-card__head"><span>{t.available}</span><small>{phaseLabel(unit.phase)}</small></div><h2>{t.unit} №{unit.number}</h2><strong>{unit.rooms} · {unit.area} m²</strong><dl><div><dt>{t.floor}</dt><dd>{unit.floor}/{unit.maxFloor}</dd></div><div><dt>{t.entrance}</dt><dd>{unit.entrance}</dd></div><div><dt>{t.perM2}</dt><dd>{formatPrice(Math.round(unit.price / unit.area), language)}</dd></div></dl><div className="fouru-unit-card__price"><strong>{formatPrice(unit.price, language)}</strong><del>{formatPrice(unit.oldPrice, language)}</del></div><div className="fouru-unit-card__actions"><button type="button" onClick={() => setPlanbox(unit)}>{t.details}</button><button type="button" onClick={() => consult(unit)}>{t.consult}</button></div></article>)}{!filtered.length ? <p className="fouru-empty">{t.empty}</p> : null}{visible < filtered.length ? <div className="fouru-show-more"><button type="button" onClick={() => setVisible((value) => value + 12)}>{t.showMore}<span>↓</span></button><small>{t.shown} {Math.min(visible, filtered.length)} {t.of} {filtered.length}</small></div> : null}</section> : <section className="fouru-chess-layout"><div className="fouru-chess-main"><div className="fouru-chess-controls"><p>{t.tableHelp}</p><div><button type="button" aria-label={t.left} onClick={() => scrollChess(-420)}>←</button><button type="button" aria-label={t.right} onClick={() => scrollChess(420)}>→</button></div></div><div className="fouru-chess-scroll" ref={tableRef} tabIndex={0} onKeyDown={onChessKeyDown} aria-label={t.tableHelp}><table><thead><tr><th>{t.floor}</th>{columns.map((column) => { const [phaseName, entrance] = column.split('|'); return <th key={column}><span>{phaseLabel(phaseName)}</span><small>{t.entrance} {entrance}</small></th>; })}</tr></thead><tbody>{floors.map((floor) => <tr key={floor}><th>{floor}</th>{columns.map((column) => { const [phaseName, entrance] = column.split('|'); const units = filtered.filter((unit) => unit.floor === floor && unit.phase === phaseName && unit.entrance === entrance); return <td key={column}>{units.map((unit) => <button type="button" key={unit.id} className={effectiveSelected?.id === unit.id ? 'is-selected' : ''} onClick={() => setSelected(unit)} aria-label={`${t.unit} №${unit.number}, ${unit.area} m², ${formatPrice(unit.price, language)}`}><span>№{unit.number}</span><strong>{unit.area} m²</strong><small>{unit.rooms}</small><em>{formatPrice(unit.price, language)}</em></button>)}</td>; })}</tr>)}</tbody></table></div></div><aside className="fouru-chess-detail">{effectiveSelected ? <UnitDetail unit={effectiveSelected} language={language} compact onPlan={() => setPlanbox(effectiveSelected)} onConsult={() => consult(effectiveSelected)} /> : <p>{t.noSelection}</p>}</aside></section>}

    <aside className="fouru-snapshot-note"><span>30·08·26</span><div><strong>{t.update}</strong><p>{t.snapshotNote}</p></div></aside>
    <footer className="fouru-catalog-footer"><a className="fouru-logo" href={withLanguage('/4u', language)}><b>4U</b><span>TASHKENT</span></a><p>NRG-BI × AL-BINA</p><a href={withLanguage('/4u', language)}>{t.back} ↗</a></footer>
    {planbox ? <PlanLightbox unit={planbox} language={language} onClose={() => setPlanbox(null)} onConsult={() => consult(planbox)} /> : null}
    <LeadModal open={lead} onClose={() => setLead(false)} language={language} context={`${t.context}${effectiveSelected ? ` · unit ${effectiveSelected.number} · ${effectiveSelected.id} · ${effectiveSelected.phase}` : ''}`} brandName="4U" projectName="TASHKENT" tagline={effectiveSelected ? `${effectiveSelected.rooms} · ${effectiveSelected.area} m² · №${effectiveSelected.number}` : t.lead} facts={effectiveSelected ? [`${effectiveSelected.area} m²`, `${effectiveSelected.floor}/${effectiveSelected.maxFloor} ${t.floor.toLowerCase()}`, formatPrice(effectiveSelected.price, language)] : [t.snapshot, `${snapshotData.officialTotalAtCapture} official`, `${snapshotData.units.length} local`]} submitUrl={leadSubmitUrl()} projectSlug="4u" unitId={effectiveSelected?.id} privacyUrl={`${appBasePath}/privacy?project=4u&lang=${language}&from=catalog`} requireConsent />
  </main>;
}
